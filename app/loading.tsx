import { LoadingScreen } from "@/components/LoadingScreen";

// Shown automatically during navigation and server-side data fetching for
// any route under app/. See components/LoadingScreen.tsx.
export default function Loading() {
  return <LoadingScreen />;
}
